import { HostMapView } from '@/components/map/HostMapView';

// Native (iOS/Android) version with react-native-maps
// For web, see HomeScreen.web.tsx
export default function HomeScreen() {
  return <HostMapView />;
}
